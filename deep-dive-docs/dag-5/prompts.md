# prompts

hier gebruik ik cursor weer

* remove the stream_end_time_utc from the code, api and database, remake the database to not have stream_end_time_utc either.

Ik heb nu geleerd dat dit pas maandelijks vernieuwd word. terug naar Mistral-vibe. ik kijk of het geven van dezelfde prompt werkt.

* remove the stream_end_time_utc from the code, api and database, remake the database to not have stream_end_time_utc either.

en nu werkt het weer. wil ik nog proberen een verwijder knop te maken.

* could you also add a way to remove a stream from the schedule, add the button to the right of the link in the schedule, on a seperate column
